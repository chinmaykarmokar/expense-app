import "package:flutter/material.dart";

class EditEntryUI extends StatelessWidget {
  const EditEntryUI({ super.key });

  @override
  Widget build(BuildContext context) {
    return const Text("Edit Entry UI");
  }
}